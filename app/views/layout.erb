<!DOCTYPE html>
<html lang="en">
<head>
  <!-- 
    normalize.css remueve las diferenecia que hay entre browsers segun los defaults de css predeterminados que utilizan. 
    Revisa: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css este es el archivo donde van tus estilos, tu css
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>
   
  <div class="warning">
    <% if session[:error] %>
      <p><%= session[:error] %></p>
      <% session[:error].clear %>
    <% end %>
  </div>
   
  <div class="warning">
    <% if session[:incorrect_login] %>
      <p><%= session[:incorrect_login] %></p>
      <% session[:incorrect_login].clear %>
    <% end %>
  </div>

  <div class="warning">
    <% if session[:incorrect_user] %>
      <p><%= session[:incorrect_user] %></p>
      <% session[:incorrect_user].clear %>
    <% end %>
  </div>

 <div class="correct">
    <% if session[:logout] %>
      <p><%= session[:logout] %></p>
      <% session[:logout].clear %>
    <% end %>
  </div>

  <div class="correct">
    <% if session[:correct_user] %>
      <p><%= session[:correct_user] %></p>
      <% session[:correct_user].clear %>
    <% end %>
  </div>

  <div class="container">
      <%= yield %>
  </div>

</body>
</html>
